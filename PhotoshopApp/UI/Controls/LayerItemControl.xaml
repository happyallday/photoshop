<UserControl x:Class="PhotoshopApp.UI.Controls.LayerItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="200">
    <Border Style="{StaticResource LayerItemBorder}">
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Visibility Toggle -->
            <CheckBox x:Name="VisibilityCheckBox" 
                      IsChecked="{Binding IsVisible}" 
                      VerticalAlignment="Center"
                      Margin="0,0,8,0"/>
            
            <!-- Thumbnail -->
            <Border Grid.Column="1" 
                    Width="32" Height="32" 
                    Background="#EEEEEE" 
                    BorderBrush="#CCCCCC" 
                    BorderThickness="1"
                    Margin="0,0,8,0">
                <Image x:Name="ThumbnailImage"
                       Source="{Binding Thumbnail}"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
            </Border>
            
            <!-- Layer Name -->
            <TextBlock Grid.Column="1" 
                       Grid.ColumnSpan="2"
                       Text="{Binding Name}" 
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="48,0,0,0"
                       Foreground="#333333"/>
            
            <!-- Lock Indicator -->
            <TextBlock Grid.Column="2" 
                       Text="🔒" 
                       FontSize="12"
                       Visibility="{Binding IsLocked, Converter={StaticResource BoolToVisibilityConverter}}"
                       VerticalAlignment="Center"
                       Margin="4,0,8,0"/>
        </Grid>
    </Border>
</UserControl>